type Programe
  @model
  @auth(rules: [{allow: owner}]) {
  id: ID!
  proCode:String!
  title: String!

}


type Campus
  @model
  @auth(rules: [{allow: owner}]) {
  id: ID!
  campusID: String!
  campusName: String
  title: String!

}

type Student
  @model
  @auth(rules: [{allow: owner}]) {
  id :ID!
  stuID: String!
  studyYaer: String
  class: String!

}